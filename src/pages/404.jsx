import { Link } from 'gatsby';
import React from 'react';

const NotFound = () => {
  return (
    <div>
      NotFound
      <Link to='/'>Go to Home</Link>
    </div>
  );
};

export default NotFound;
